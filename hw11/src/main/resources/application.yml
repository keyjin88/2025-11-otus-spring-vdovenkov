spring:
  main:
    banner-mode: console
  mongodb:
    # Явный URI — самый надёжный способ передать учётные данные в Spring Boot 4
    uri: mongodb://root:password@localhost:27017/library?authSource=admin

server:
  port: 8080

logging:
  level:
    root: info
